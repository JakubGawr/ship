import { ActiveCellDirective } from './active-cell.directive';

describe('ActiveCellDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveCellDirective();
    expect(directive).toBeTruthy();
  });
});
