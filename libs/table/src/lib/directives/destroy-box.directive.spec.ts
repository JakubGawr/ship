import { DestroyBoxDirective } from './destroy-box.directive';

describe('DestroyBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyBoxDirective();
    expect(directive).toBeTruthy();
  });
});
